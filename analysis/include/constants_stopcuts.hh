#ifndef CONSTANTS_STOPCUTS_HH
#define CONSTANTS_STOPCUTS_HH

// standard susy QCD cut
static const float MIN_DPHI_JET_MET = 0.4; 

// both monojet and c-tagged group seem to confirm we should use 
// only the first 4 jets
static const size_t DPHI_JET_MET_NJET = 4; 

#endif 
