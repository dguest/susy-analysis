#ifndef REGION_LOGIC_HH
#define REGION_LOGIC_HH

#include "region_enums.hh"

namespace reg {
  bool onelep_region(Selection);
  bool twolep_region(Selection);
  bool lepton_region(Selection);
}
#endif
